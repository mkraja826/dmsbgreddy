// Optional runtime DMS config for deployed website.
// If your deployed site still says DMS not configured after setting .env,
// fill these values and redeploy. Supabase anon key is safe in frontend when RLS is enabled.
window.DMS_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
